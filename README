RAMDISK

An in-memory filesystem. Instead of reading and writing disk blocks, the RAMDISK filesystem will use main memory for storage.

I am currently working on improving the memory utilization by holding block metadata in a completely separate data structure.
